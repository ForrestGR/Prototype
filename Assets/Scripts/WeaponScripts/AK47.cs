using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AK47 : Weapon
{
    //[SerializeField] private float fireRate = 0.1f; // Feuerrate in Sekunden zwischen den Schüssen
    //private float nextTimeToFire = 0f;

    //protected override void Start()
    //{
    //    //magazineCapacity = 30; // Setzt die maximale Magazinkapazität der AK47
    //    //totalAmmo = 180; // Setzt die Gesamtmunition der AK47
    //    //currentAmmo = magazineCapacity; // Initialisiert die aktuelle Munition mit der maximalen Munition
    //    //base.Start(); // Ruft die Start-Methode der Basisklasse auf
    //}

    //protected override void Update()
    //{
    //    //base.Update(); // Ruft die Update-Methode der Basisklasse auf

    //    // Weitere AK47-spezifische Updates können hier hinzugefügt werden
    //}

    ////public override void Shoot()
    ////{
    ////    if (Time.time >= nextTimeToFire)
    ////    {
    ////        nextTimeToFire = Time.time + fireRate;
    ////        base.Shoot();
    ////    }
    ////}
}
